logging:
  level:
    root: debug

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

external:
  github-api:
    client-id: a
    client-secret: a
    redirect-uri: http://localhost:8080/oauth2/callback/github
    scrop: "read:user user:email"
