feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

spring:
  main:
    allow-bean-definition-overriding: true
  data:
    mongodb:
      host: localhost
      port: 27017
      username: admin
      password: local
      database: review-local
      authentication-database: admin

external:
  naver-api:
    id: a
    secret: a

tecobrary:
  authentication:
    encrypt:
      key: testt12345testt12345testt1234567
    jwt:
      issuer: "test"
      subject: "test"
      scope: "test"
      version: "test"
      secret-key: "test"